# 🚀 Деплой на Fly.io

Пошаговая инструкция по развертыванию Telegram Translation Bot на Fly.io.

## 📋 Предварительные требования

1. **GitHub репозиторий** - загрузите код в GitHub
2. **Аккаунт Fly.io** - зарегистрируйтесь на [fly.io](https://fly.io) (бесплатно)
3. **Токен бота** - получите от [@BotFather](https://t.me/BotFather)

## 🔑 Почему Fly.io лучше чем Render (для ботов)?

### ✅ Преимущества Fly.io:
- **Не засыпает** - бесплатный план работает 24/7 без засыпания
- **Быстрый старт** - нет холодного старта, бот всегда готов
- **Простой деплой** - через GitHub или CLI
- **Глобальная сеть** - можно выбрать регион ближе к пользователям
- **256MB RAM бесплатно** - достаточно для бота

### ⚠️ Render (бесплатный план):
- Засыпает после 15 минут неактивности
- Холодный старт ~30 секунд
- Может отключаться при неактивности

## 🚀 Шаг 1: Установка Fly CLI

```bash
# macOS/Linux
curl -L https://fly.io/install.sh | sh

# Или через Homebrew (macOS)
brew install flyctl

# Проверка установки
fly version
```

## 🔐 Шаг 2: Авторизация

```bash
# Войдите в аккаунт Fly.io
fly auth login

# Откроется браузер для авторизации
```

## 📦 Шаг 3: Создание приложения

```bash
# Перейдите в папку проекта
cd /path/to/telegram-translator

# Создайте приложение (если еще не создано)
fly apps create telegram-translator-bot

# Или используйте существующий fly.toml
fly launch
```

## 🔧 Шаг 4: Настройка переменных окружения

```bash
# Обязательные переменные
fly secrets set BOT_TOKEN=your_bot_token_from_botfather

# Переводчик (выберите один)
fly secrets set TRANSLATOR_PROVIDER=ARGOS  # или DEEPL, GOOGLE, LIBRE, MYMEMORY

# Для DEEPL (если используете)
fly secrets set DEEPL_API_KEY=your_deepl_key

# Для GOOGLE (если используете)
fly secrets set GOOGLE_PROJECT_ID=your_project_id
fly secrets set GOOGLE_CREDENTIALS_JSON_PATH=/app/credentials.json

# Для LIBRE (если используете)
fly secrets set LIBRE_BASE_URL=https://libretranslate.com
fly secrets set LIBRE_API_KEY=your_libre_key  # опционально

# База данных (SQLite по умолчанию, или PostgreSQL)
fly secrets set DATABASE_URL=sqlite:///data/bot.db

# Дополнительные настройки (опционально)
fly secrets set MODE=polling
fly secrets set LOG_LEVEL=INFO
fly secrets set DEFAULT_CHANNEL_LANGS=en
fly secrets set DEFAULT_USER_LANG=en
```

## 🗄️ Шаг 5: Настройка базы данных (опционально)

### Вариант 1: SQLite (проще, бесплатно)
- SQLite работает "из коробки"
- Данные хранятся в volume
- Подходит для небольших проектов

```bash
# Создайте volume для данных
fly volumes create bot_data --size 1 --region iad

# Обновите fly.toml для монтирования volume
```

### Вариант 2: PostgreSQL (рекомендуется для продакшена)
- Используйте внешний PostgreSQL (например, Supabase, Neon, или Fly PostgreSQL)
- Или создайте PostgreSQL на Fly.io (платно)

## 🚀 Шаг 6: Деплой

### Через GitHub (автоматический):
1. Подключите GitHub репозиторий в [Fly.io Dashboard](https://fly.io/dashboard)
2. Выберите репозиторий и ветку
3. Fly.io автоматически деплоит при каждом push

### Через CLI (ручной):
```bash
# Деплой
fly deploy

# Проверка статуса
fly status

# Просмотр логов
fly logs
```

## ✅ Шаг 7: Проверка работы

```bash
# Проверка health check
fly open /health

# Или через curl
curl https://telegram-translator-bot.fly.dev/health

# Просмотр логов
fly logs

# Проверка статуса
fly status
```

## 🔄 Обновление

### Автоматическое (через GitHub):
1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub
3. Fly.io автоматически пересоберет и задеплоит

### Ручное:
```bash
# Деплой последних изменений
fly deploy

# Откат к предыдущей версии
fly releases
fly releases rollback <release-id>
```

## 📊 Мониторинг

```bash
# Просмотр метрик
fly metrics

# Просмотр логов в реальном времени
fly logs

# SSH подключение к машине
fly ssh console

# Проверка статуса
fly status
```

## 🛠️ Полезные команды

```bash
# Просмотр всех секретов
fly secrets list

# Удаление секрета
fly secrets unset SECRET_NAME

# Масштабирование
fly scale count 1  # количество машин
fly scale memory 256  # память в MB

# Перезапуск
fly apps restart telegram-translator-bot

# Удаление приложения
fly apps destroy telegram-translator-bot
```

## ⚙️ Настройка fly.toml

Файл `fly.toml` уже создан с оптимальными настройками:
- Регион: `iad` (Washington DC) - измените на ближайший к вашим пользователям
- Память: 256MB (бесплатно)
- Health check: `/health`
- Автозапуск: включен

### Изменение региона:
```bash
# Список доступных регионов
fly regions list

# Изменение региона в fly.toml
# primary_region = "fra"  # Frankfurt
# primary_region = "ams"  # Amsterdam
# primary_region = "sin"  # Singapore
```

## 🆘 Решение проблем

### Бот не отвечает:
1. Проверьте логи: `fly logs`
2. Убедитесь, что `BOT_TOKEN` правильный: `fly secrets list`
3. Проверьте health check: `fly open /health`

### Ошибки деплоя:
1. Проверьте Dockerfile
2. Убедитесь, что все зависимости в requirements.txt
3. Проверьте логи сборки: `fly logs --build`

### Проблемы с базой данных:
1. Для SQLite: убедитесь, что volume создан
2. Для PostgreSQL: проверьте `DATABASE_URL`

## 💰 Стоимость

### Бесплатный план Fly.io:
- ✅ 3 shared-cpu-1x machines (256MB RAM каждая)
- ✅ 160GB исходящий трафик/месяц
- ✅ Не засыпает, работает 24/7
- ✅ Глобальная сеть

**Для бота этого более чем достаточно!**

## 📞 Поддержка

- **Fly.io Docs**: [fly.io/docs](https://fly.io/docs)
- **Fly.io Community**: [community.fly.io](https://community.fly.io)
- **GitHub Issues**: создайте issue в репозитории проекта

---

🎉 **Готово!** Ваш бот теперь работает 24/7 на Fly.io без засыпания!

