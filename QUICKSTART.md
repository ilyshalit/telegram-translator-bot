# 🚀 Быстрый запуск Telegram Translation Bot

## Минимальная настройка (5 минут)

### 1. Создайте бота
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям и получите токен

### 2. Получите API ключ DeepL (бесплатно)
1. Зарегистрируйтесь на [DeepL](https://www.deepl.com/pro-api)
2. Получите бесплатный API ключ (500,000 символов/месяц)

### 3. Запустите бота

#### Вариант A: Docker (рекомендуется)
```bash
# Клонируйте репозиторий
git clone <repository-url>
cd telegram-translator

# Создайте .env файл
cat > .env << EOF
BOT_TOKEN=your_bot_token_here
TRANSLATOR_PROVIDER=DEEPL
DEEPL_API_KEY=your_deepl_key_here
EOF

# Запустите
docker-compose up -d
```

#### Вариант B: Локально
```bash
# Установите Python 3.11+
pip install -r requirements.txt

# Создайте .env файл (как выше)

# Запустите
python -m app.main
```

### 4. Настройте канал

1. **Включите обсуждения:**
   - Настройки канала → Обсуждения → Включить

2. **Добавьте бота:**
   - Добавить администраторов → Найти бота
   - Права: "Публиковать сообщения" + "Просматривать сообщения"

3. **Настройте языки:**
   ```
   /set_channel_langs en,ru
   ```

### 5. Тестирование

1. **В ЛС боту:**
   ```
   Привет мир
   ```
   Ответ: `🌐 Russian → English: Hello world`

2. **В канале:**
   - Опубликуйте пост с текстом
   - Бот автоматически добавит комментарий с переводом

3. **В комментариях:**
   ```
   @your_bot_name How are you?
   ```
   Ответ: `🌐 English → Russian: Как дела?`

## Готово! 🎉

Ваш бот работает. Полная документация в [README.md](README.md).

## Быстрые команды

```bash
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Обновление
git pull && docker-compose up -d --build

# Статус
curl http://localhost:8080/health
```

## Проблемы?

1. **Бот не отвечает** → Проверьте токен в .env
2. **Нет переводов** → Проверьте DEEPL_API_KEY
3. **Не работает в канале** → Проверьте права бота и обсуждения
4. **Другие проблемы** → См. раздел "Устранение неполадок" в README.md

