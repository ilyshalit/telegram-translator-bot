# Telegram Translation Bot

Продакшн-готовый Telegram-бот для автоматического перевода сообщений в каналах и комментариях с поддержкой множественных провайдеров переводов.

## 🌟 Возможности

### Основной функционал
- **Автоперевод постов канала** - автоматически переводит новые посты и публикует переводы в комментариях
- **Переводы в ЛС** - отправьте любой текст боту и получите перевод
- **Переводы в комментариях** - ответьте боту или упомяните его для получения перевода
- **Поддержка редактирования** - обновляет переводы при редактировании постов

### Провайдеры переводов
- **DeepL** - высокое качество переводов (рекомендуется)
- **Google Translate** - через Google Cloud API
- **LibreTranslate** - открытый источник, можно развернуть самостоятельно

### Безопасность и надежность
- **Rate limiting** - защита от спама и злоупотреблений
- **PII-safe логирование** - токены и личные данные не логируются
- **Graceful shutdown** - корректное завершение работы
- **Health checks** - мониторинг состояния сервиса
- **Fallback между провайдерами** - автоматическое переключение при сбоях

### Настройки и локализация
- **Персональные языки** - каждый пользователь может выбрать свой язык
- **Настройки канала** - администраторы настраивают целевые языки
- **Автоопределение языка** - умное определение исходного языка
- **Многоязычный интерфейс** - поддержка русского и английского

## 🚀 Быстрый старт

### 1. Создание бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Настройте команды бота:
```
start - Начать работу с ботом
help - Показать справку
set_my_lang - Установить язык перевода
privacy - Политика конфиденциальности
provider - Информация о провайдере
set_channel_langs - [Админ] Настроить языки канала
toggle_autotranslate - [Админ] Включить/выключить автоперевод
stats - [Админ] Статистика переводов
```

### 2. Настройка канала

1. **Включите обсуждения в канале:**
   - Перейдите в настройки канала
   - Включите "Обсуждения" (Discussions)
   - Это необходимо для публикации комментариев-переводов

2. **Добавьте бота в канал:**
   - Добавьте бота как администратора
   - Выдайте права: "Публиковать сообщения" и "Просматривать сообщения"
   - Другие права не требуются

3. **Настройте языки перевода:**
   ```
   /set_channel_langs en,ru,tr
   ```

### 3. Установка и запуск

#### Через Docker (рекомендуется)

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd telegram-translator
```

2. **Настройте переменные окружения:**
```bash
cp env.example .env
# Отредактируйте .env файл
```

3. **Запустите через Docker Compose:**
```bash
docker-compose up -d
```

#### Локальная установка

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Настройте переменные окружения:**
```bash
cp env.example .env
# Отредактируйте .env файл
```

3. **Запустите бота:**
```bash
# Polling режим
python -m app.main

# Webhook режим
uvicorn app.main:app --host 0.0.0.0 --port 8080
```

## ⚙️ Конфигурация

### Обязательные переменные

```env
# Токен бота от BotFather
BOT_TOKEN=your_bot_token_here

# Провайдер переводов
TRANSLATOR_PROVIDER=DEEPL  # DEEPL | GOOGLE | LIBRE
```

### Настройка провайдеров

#### DeepL (рекомендуется)
```env
DEEPL_API_KEY=your_deepl_api_key_here
```

Получите бесплатный API ключ на [DeepL](https://www.deepl.com/pro-api).

#### Google Translate
```env
GOOGLE_PROJECT_ID=your_google_project_id
GOOGLE_CREDENTIALS_JSON_PATH=/path/to/credentials.json
```

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите Cloud Translation API
3. Создайте Service Account и скачайте JSON ключ
4. Укажите путь к файлу ключа

#### LibreTranslate
```env
LIBRE_BASE_URL=https://libretranslate.com
LIBRE_API_KEY=your_libre_api_key  # Опционально
```

Можете использовать публичный сервис или развернуть [собственный](https://github.com/LibreTranslate/LibreTranslate).

### Дополнительные настройки

```env
# Языки по умолчанию
DEFAULT_CHANNEL_LANGS=en
DEFAULT_USER_LANG=en

# Режим работы
MODE=polling  # polling | webhook
WEBHOOK_URL=https://your-domain.com  # Для webhook режима
PORT=8080

# Rate limiting
RATE_LIMIT_REQUESTS=5  # Запросов на пользователя
RATE_LIMIT_WINDOW=15   # За период в секундах

# Логирование
LOG_LEVEL=INFO

# Sentry (опционально)
USE_SENTRY=false
SENTRY_DSN=your_sentry_dsn_here
```

## 📖 Использование

### Команды в ЛС

- `/start` - Начать работу с ботом
- `/help` - Подробная справка
- `/set_my_lang ru` - Установить русский как целевой язык
- `/privacy` - Политика конфиденциальности
- `/provider` - Информация о текущем провайдере

### Команды администратора в канале

- `/set_channel_langs en,ru,tr` - Установить целевые языки
- `/toggle_autotranslate on` - Включить автоперевод
- `/toggle_autotranslate off` - Выключить автоперевод
- `/stats` - Показать статистику переводов

### Примеры использования

#### В ЛС:
```
Пользователь: Hello world
Бот: 🌐 English → Russian:

Привет мир
```

#### Указание языка:
```
Пользователь: переведи на en: Привет мир
Бот: 🌐 Russian → English:

Hello world
```

#### В комментариях:
```
Пользователь: @translator_bot Как дела?
Бот: 🌐 Russian → English:

How are you?
```

## 🏗️ Архитектура

```
app/
├── __init__.py
├── main.py              # Точка входа
├── bot.py               # Инициализация бота
├── handlers/            # Обработчики сообщений
│   ├── private.py       # ЛС команды
│   ├── channel.py       # Автоперевод каналов
│   └── comments.py      # Комментарии
├── core/                # Основная логика
│   ├── config.py        # Конфигурация
│   ├── logger.py        # Безопасное логирование
│   ├── translate.py     # Сервис переводов
│   ├── storage.py       # База данных
│   ├── i18n.py          # Локализация
│   ├── rate_limit.py    # Rate limiting
│   └── utils.py         # Утилиты
├── middlewares/         # Middleware
│   ├── throttling.py    # Ограничение запросов
│   └── auth.py          # Авторизация
├── models/              # Модели данных
│   ├── channel_settings.py
│   └── user_settings.py
└── migrations/          # Миграции БД
```

## 🐳 Деплой

### Railway
```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в аккаунт
railway login

# Создайте проект
railway init

# Установите переменные окружения
railway variables set BOT_TOKEN=your_token
railway variables set TRANSLATOR_PROVIDER=DEEPL
railway variables set DEEPL_API_KEY=your_key
railway variables set MODE=webhook
railway variables set WEBHOOK_URL=https://your-app.railway.app

# Деплой
railway up
```

### Fly.io
```bash
# Установите Fly CLI
curl -L https://fly.io/install.sh | sh

# Войдите в аккаунт
fly auth login

# Создайте приложение
fly apps create telegram-translator

# Установите переменные
fly secrets set BOT_TOKEN=your_token
fly secrets set TRANSLATOR_PROVIDER=DEEPL
fly secrets set DEEPL_API_KEY=your_key
fly secrets set MODE=webhook

# Деплой
fly deploy
```

### VPS с Docker
```bash
# На сервере
git clone <repository-url>
cd telegram-translator

# Настройте .env
cp env.example .env
nano .env

# Запустите
docker-compose up -d

# Настройте nginx для HTTPS (если нужен webhook)
```

## 🔧 Разработка

### Установка для разработки
```bash
git clone <repository-url>
cd telegram-translator

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt

# Настройте pre-commit хуки
pip install pre-commit
pre-commit install
```

### Запуск тестов
```bash
# Все тесты
pytest

# С покрытием
pytest --cov=app

# Конкретный тест
pytest tests/test_lang_detect.py -v
```

### Линтинг и форматирование
```bash
# Форматирование
black app/ tests/

# Линтинг
flake8 app/ tests/

# Проверка типов
mypy app/
```

## 🐛 Устранение неполадок

### Бот не отвечает в канале

1. **Проверьте права бота:**
   - Бот должен быть администратором
   - Нужны права "Публиковать сообщения"

2. **Проверьте обсуждения:**
   - В настройках канала должны быть включены "Обсуждения"

3. **Проверьте логи:**
   ```bash
   docker-compose logs telegram-bot
   ```

### Переводы не работают

1. **Проверьте API ключи:**
   ```bash
   # В логах должно быть
   # "Available providers: ['DEEPL']"
   ```

2. **Проверьте лимиты провайдера:**
   - DeepL Free: 500,000 символов/месяц
   - Google: Платно после бесплатного лимита

3. **Проверьте подключение:**
   ```bash
   # Тест DeepL API
   curl -X POST 'https://api-free.deepl.com/v2/translate' \
   -H 'Authorization: DeepL-Auth-Key YOUR_KEY' \
   -d 'text=Hello&target_lang=RU'
   ```

### Rate limiting срабатывает слишком часто

Увеличьте лимиты в `.env`:
```env
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW=30
```

### Проблемы с базой данных

```bash
# Проверьте файл БД
ls -la bot.db

# Пересоздайте БД (потеряете данные!)
rm bot.db
# Перезапустите бота
```

## 📊 Мониторинг

### Health Check
```bash
curl http://localhost:8080/health
```

Ответ:
```json
{
  "status": "healthy",
  "database": "ok",
  "bot": "ok"
}
```

### Логи
```bash
# Docker
docker-compose logs -f telegram-bot

# Локально
tail -f logs/bot.log
tail -f logs/error.log
```

### Метрики
Бот поддерживает интеграцию с Sentry для мониторинга ошибок:

```env
USE_SENTRY=true
SENTRY_DSN=https://your-sentry-dsn
```

## 🔒 Безопасность

### Что логируется
- Системные события (запуск, остановка)
- Ошибки переводов (без содержимого)
- Статистика использования (анонимно)

### Что НЕ логируется
- Содержимое сообщений пользователей
- Токены и API ключи
- ID пользователей и имена

### Рекомендации
1. Используйте HTTPS для webhook режима
2. Регулярно обновляйте зависимости
3. Мониторьте логи на предмет подозрительной активности
4. Используйте отдельные API ключи для разных окружений

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

- 🐛 [Issues](https://github.com/your-repo/issues) - сообщения об ошибках
- 💡 [Discussions](https://github.com/your-repo/discussions) - вопросы и идеи
- 📧 Email: your-email@example.com

## 🎯 Roadmap

- [ ] Поддержка голосовых сообщений
- [ ] Веб-интерфейс для управления
- [ ] Интеграция с Telegram Mini Apps
- [ ] Поддержка документов и изображений с текстом
- [ ] Кэширование переводов
- [ ] Аналитика и детальная статистика

