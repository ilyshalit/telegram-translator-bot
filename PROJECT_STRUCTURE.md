# Структура проекта Telegram Translation Bot

## 📁 Обзор структуры

```
Тг переводчик/
├── app/                    # Основной код приложения
│   ├── __init__.py         # Инициализация пакета
│   ├── main.py             # Точка входа приложения
│   ├── bot.py              # Класс бота и инициализация
│   │
│   ├── core/               # Ядро приложения
│   │   ├── config.py       # Конфигурация (Pydantic Settings)
│   │   ├── database.py     # Работа с БД (SQLite)
│   │   ├── i18n.py         # Интернационализация (языки)
│   │   ├── logger.py       # Логирование
│   │   ├── rate_limit.py   # Ограничение частоты запросов
│   │   ├── translate.py    # Сервис переводов (множество провайдеров)
│   │   └── utils.py        # Вспомогательные функции
│   │
│   ├── handlers/           # Обработчики сообщений
│   │   ├── __init__.py     # Экспорт роутеров
│   │   ├── private.py      # Команды в ЛС (/start, /help, /menu, etc.)
│   │   ├── channel.py      # Обработка постов в каналах
│   │   ├── comments.py     # Обработка комментариев
│   │   ├── group_events.py # События добавления/удаления бота
│   │   └── menu.py         # Обработка меню и кнопок
│   │
│   ├── middlewares/        # Промежуточное ПО
│   │   ├── __init__.py     # Экспорт middleware
│   │   ├── auth.py         # Проверка прав доступа
│   │   └── throttling.py   # Ограничение частоты запросов
│   │
│   └── models/             # Модели данных (Pydantic)
│       ├── __init__.py     # Экспорт моделей
│       ├── user_settings.py    # Модель настроек пользователя
│       └── channel_settings.py # Модель настроек канала
│
├── logs/                   # Логи приложения
│   ├── bot.log            # Основной лог
│   └── error.log          # Лог ошибок
│
├── bot.db                  # База данных SQLite
│
├── requirements.txt        # Зависимости Python
├── pyproject.toml          # Конфигурация проекта
├── Dockerfile              # Docker образ
├── docker-compose.yml      # Docker Compose конфигурация
│
├── .env                    # Переменные окружения (не в git)
├── env.example             # Пример .env файла
│
├── start.py                # Скрипт запуска для Render
├── reset_and_start.py      # Скрипт сброса webhook и запуска
│
├── README.md               # Основная документация
├── QUICKSTART.md           # Быстрый старт
├── QUICK_DEPLOY.md         # Быстрое развертывание
├── RENDER_DEPLOY.md        # Развертывание на Render
│
├── render.yaml             # Конфигурация Render
└── render.env              # Переменные для Render
```

## 📂 Описание папок и файлов

### `app/` - Основной код

#### `app/main.py`
- **Назначение**: Точка входа приложения
- **Функции**: 
  - Инициализация бота
  - Обработка сигналов (SIGINT, SIGTERM)
  - Запуск в режиме polling или webhook

#### `app/bot.py`
- **Назначение**: Класс бота и конфигурация
- **Класс**: `TranslationBot`
- **Функции**:
  - Инициализация бота и диспетчера
  - Настройка middleware
  - Регистрация роутеров
  - Установка команд бота
  - Обработка startup/shutdown

### `app/core/` - Ядро приложения

#### `app/core/config.py`
- **Назначение**: Конфигурация приложения
- **Класс**: `Settings` (Pydantic BaseSettings)
- **Параметры**:
  - `bot_token` - Токен Telegram бота
  - `translator_provider` - Провайдер переводов (ARGOS, DEEPL, GOOGLE, LIBRE, MYMEMORY)
  - `database_url` - URL базы данных
  - `rate_limit_requests` - Лимит запросов
  - И другие настройки

#### `app/core/database.py`
- **Назначение**: Работа с базой данных
- **Класс**: `UniversalStorage`
- **Функции**:
  - Инициализация БД (SQLite)
  - CRUD операции для настроек пользователей и каналов
  - Статистика переводов
  - Связь пользователей с каналами

#### `app/core/i18n.py`
- **Назначение**: Интернационализация
- **Функции**:
  - Определение языка пользователя
  - Локализованные строки (русский/английский)
  - Парсинг списков языков
  - Нормализация кодов языков

#### `app/core/translate.py`
- **Назначение**: Сервис переводов
- **Класс**: `TranslationService`
- **Провайдеры**:
  - `ARGOS` - Open-source, бесплатный, хорошее качество
  - `DEEPL` - Высокое качество, 500k символов/месяц бесплатно
  - `GOOGLE` - Хорошее качество, требует настройку
  - `LIBRE` - Open-source, можно использовать публичный сервер
  - `MYMEMORY` - Базовое качество, полностью бесплатный
- **Функции**:
  - Перевод текста с fallback на другие провайдеры
  - Определение языка текста
  - Перевод на несколько языков одновременно

#### `app/core/rate_limit.py`
- **Назначение**: Ограничение частоты запросов
- **Класс**: `MultiLevelRateLimiter`
- **Уровни**:
  - Пользовательский
  - Чат-уровень
  - Глобальный (защита от DDoS)

#### `app/core/utils.py`
- **Назначение**: Вспомогательные функции
- **Функции**:
  - `extract_text_from_message()` - Извлечение текста из сообщения
  - `split_long_message()` - Разделение длинных сообщений
  - `normalize_text()` - Нормализация текста
  - `format_translation_comment()` - Форматирование комментариев с переводами
  - И другие утилиты

### `app/handlers/` - Обработчики сообщений

#### `app/handlers/private.py`
- **Назначение**: Команды в личных сообщениях
- **Команды**:
  - `/start` - Приветствие и начало работы
  - `/help` - Справка
  - `/menu` - Главное меню
  - `/languages` - Выбор языка интерфейса
  - `/setup` - Инструкции по настройке
  - `/my_channels` - Список подключенных каналов
  - `/set_my_lang` - Установка языка пользователя
  - `/privacy` - Политика конфиденциальности
  - `/provider` - Информация о провайдере переводов
  - `/commands` - Список всех команд
  - `/reset` - Сброс данных пользователя
- **Callback'и**: Обработка кнопок и выборов

#### `app/handlers/channel.py`
- **Назначение**: Обработка постов в каналах
- **Функции**:
  - Автоматический перевод постов (если включен)
  - Команды админов канала (`/set_channel_langs`, `/toggle_autotranslate`, `/stats`)

#### `app/handlers/comments.py`
- **Назначение**: Обработка комментариев под постами
- **Функции**:
  - Перевод комментариев при упоминании бота
  - Ответ на комментарии бота

#### `app/handlers/group_events.py`
- **Назначение**: События добавления/удаления бота
- **Функции**:
  - Приветствие при добавлении в канал/группу
  - Сохранение связи пользователя с каналом

#### `app/handlers/menu.py`
- **Назначение**: Обработка меню и кнопок
- **Функции**:
  - Создание клавиатур меню
  - Обработка callback'ов кнопок

### `app/middlewares/` - Промежуточное ПО

#### `app/middlewares/auth.py`
- **Назначение**: Проверка прав доступа
- **Функции**:
  - Проверка прав администратора для админских команд
  - Логирование действий

#### `app/middlewares/throttling.py`
- **Назначение**: Ограничение частоты запросов
- **Функции**:
  - Проверка rate limits
  - Отправка сообщений о превышении лимита

### `app/models/` - Модели данных

#### `app/models/user_settings.py`
- **Назначение**: Модель настроек пользователя
- **Класс**: `UserSettings` (Pydantic)
- **Поля**: `user_id`, `target_lang`, `created_at`, `updated_at`

#### `app/models/channel_settings.py`
- **Назначение**: Модель настроек канала
- **Класс**: `ChannelSettings` (Pydantic)
- **Поля**: `chat_id`, `target_langs`, `autotranslate`, `created_at`, `updated_at`

## 🔧 Конфигурация

### Переменные окружения (`.env`)

```bash
# Обязательные
BOT_TOKEN=your_bot_token_here

# Переводчик (выберите один)
TRANSLATOR_PROVIDER=ARGOS  # ARGOS | DEEPL | GOOGLE | LIBRE | MYMEMORY

# Для DEEPL
DEEPL_API_KEY=your_key_here

# Для GOOGLE
GOOGLE_PROJECT_ID=your_project_id
GOOGLE_CREDENTIALS_JSON_PATH=/path/to/credentials.json

# Для LIBRE
LIBRE_BASE_URL=https://libretranslate.com
LIBRE_API_KEY=your_key_here  # Опционально

# База данных
DATABASE_URL=sqlite:///bot.db

# Настройки
DEFAULT_USER_LANG=en
DEFAULT_CHANNEL_LANGS=en
RATE_LIMIT_REQUESTS=5
RATE_LIMIT_WINDOW=15
```

## 🚀 Запуск

### Локальный запуск
```bash
python app/main.py
```

### С Docker
```bash
docker-compose up -d
```

### На Render
```bash
python start.py
```

## 📊 База данных

### Таблицы

1. **channel_settings** - Настройки каналов
   - `chat_id` (PRIMARY KEY)
   - `target_langs` - Список языков для перевода
   - `autotranslate` - Включен ли автоперевод
   - `created_at`, `updated_at` - Временные метки

2. **user_settings** - Настройки пользователей
   - `user_id` (PRIMARY KEY)
   - `target_lang` - Предпочитаемый язык
   - `created_at`, `updated_at` - Временные метки

3. **stats** - Статистика переводов
   - `id` (PRIMARY KEY)
   - `date` - Дата
   - `channel_id` - ID канала
   - `posts` - Количество постов
   - `translations` - Количество переводов
   - `created_at` - Временная метка

4. **user_channels** - Связь пользователей с каналами
   - `id` (PRIMARY KEY)
   - `user_id` - ID пользователя
   - `channel_id` - ID канала
   - `channel_title` - Название канала
   - `added_at` - Временная метка
   - UNIQUE(user_id, channel_id)

## 🔄 Поток данных

1. **Сообщение приходит** → `middlewares/throttling.py` проверяет rate limit
2. **Проверка прав** → `middlewares/auth.py` проверяет права доступа
3. **Роутинг** → Диспетчер направляет в соответствующий handler
4. **Обработка** → Handler обрабатывает сообщение
5. **Перевод** → `core/translate.py` переводит текст
6. **Сохранение** → `core/database.py` сохраняет данные
7. **Ответ** → Бот отправляет ответ пользователю

## 🛠️ Улучшения и оптимизации

### Выполнено:
- ✅ Удален дубликат `storage.py` (используется `database.py`)
- ✅ Объединен `new_commands.py` с `private.py`
- ✅ Упрощена реализация PostgreSQL (не используется)
- ✅ Добавлен провайдер Argos Translate

### Рекомендации:
- 📝 Модели в `models/` не используются напрямую - можно использовать для валидации
- 🔧 Можно добавить кэширование переводов
- 📊 Можно улучшить статистику
- 🧪 Добавить больше тестов

